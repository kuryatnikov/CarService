<Window x:Class="CarService.Windows.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarService.Windows"
        mc:Ignorable="d"
        Title="ErrorWindow" Height="150" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="Error" Margin="15" Text="{Binding}"
                   VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
        <Button Grid.Row="1" Width="100" Height="25" Content="Ok" IsDefault="True" IsCancel="True"
                Click="OkClick"></Button>
        
    </Grid>
</Window>
